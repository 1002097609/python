# 工作计划

> 营销素材拆解与裂变系统 — 按日期规划待完成的工作。

---

## 2026-05-02

### P1 — 高优先级

| # | 功能 | 说明 | 预估工时 |
|---|------|------|---------|
| 1 | **AI 辅助拆解（P3 功能启动）** | 后端新增 `POST /api/dismantle/ai-analyze`，Dismantle.vue 增加「AI智能拆解」按钮，自动填充 L1-L5 表单 | 4h |
| 2 | **裂变表单骨架预览** | Fission.vue Step 1 选中骨架后，内联展示 L3 结构名称和 L4 关键元素 | 2h |
| 3 | **素材库批量状态更新** | MaterialLib.vue 批量操作栏增加状态变更下拉，调用 `PUT /api/material/{id}` | 1.5h |
| 4 | **L4 覆写显示骨架默认值** | Fission.vue 未勾选自定义时，以只读方式显示当前骨架的 L4 值 | 1.5h |

### P2 — 中优先级

| # | 功能 | 说明 | 预估工时 |
|---|------|------|---------|
| 5 | **裂变结果 JSON 导出** | Fission.vue 结果区增加「导出 JSON」按钮，包含结构化段落和预测数据 | 1h |
| 6 | **Dashboard 图表下钻导航** | 品类饼图→素材库筛选；骨架排名→骨架库；裂变漏斗→裂变记录 | 2h |
| 7 | **种子数据优化** | seed_data.py 裂变产出调用真实 `_generate_output`，替代占位文本 | 1.5h |
| 8 | **Dismantle.vue 结构行 CSS 修复** | structure-row 增加 `flex-wrap: wrap`，`ratio-unit` 增加 `flex-shrink: 0` | 0.5h |

### P3 — 低优先级

| # | 功能 | 说明 | 预估工时 |
|---|------|------|---------|
| 9 | **批量裂变状态流转** | 后端增加批量状态更新接口，前端支持多选后批量推进 | 2h |
| 10 | **标签编辑防误操作** | TagManage.vue 编辑态增加「未保存」提示，导航前确认 | 1h |

---

## 待评估 / 远期

| 功能 | 说明 |
|------|------|
| 素材内容全文搜索 | 后端支持按 content 全文检索，前端搜索框扩展到内容搜索 |
| 骨架评分详情页 | 独立页面展示骨架每次裂变的效果趋势和详细对比 |
| 用户操作日志 | 记录关键操作（创建/删除/状态变更）到 operation_log 表 |
| 数据导入导出 | 素材/骨架支持 JSON/CSV 批量导入导出 |
| 权限控制 | 增加用户认证，区分管理员/运营/只读角色 |
