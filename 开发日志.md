# 开发日志

## 2026-05-01（续）

### 完成的工作

#### 第一轮：8项功能完善（commit: 98dcb58）

1. **裂变记录状态流转**：后端新增 PUT /api/fission/{id}/status（含流转合法性校验），前端表格每行增加推进按钮（草稿→待审核→已采用→已投放）
2. **骨架库搜索**：后端 list_skeletons 增加 keyword 参数，前端工具栏增加搜索输入框
3. **素材跳转拆解补全**：Dismantle.vue 优先通过 API 获取完整素材数据，避免 URL 参数截断
4. **裂变结果复制增强**：增加非 HTTPS 环境的降级复制方案（textarea + execCommand）
5. **仪表盘自动刷新**：增加手动刷新按钮 + 30s/60s/5min 自动刷新选项
6. **标签编辑 option 关联**：TagManage.vue 编辑表单增加 option_id 下拉选择
7. **效果数据趋势图**：FissionRecords.vue 详情弹窗增加 ECharts ROI/CTR 双 Y 轴折线图
8. **骨架一键裂变**：验证骨架库「去裂变→」功能正常

#### 第二轮：6项优化（commit: 687d8c4）

1. **删除级联处理**：material.py 和 skeleton.py 删除接口增加级联清理，防止 FK 违反
2. **全局 API 错误拦截器**：axios response interceptor 统一处理 401/500/超时/断网
3. **页面标题动态更新**：router.afterEach 根据 meta.title 更新 document.title
4. **Dashboard 布局+空状态**：页眉增加 flex 布局；图表数据为空时显示 el-empty 占位
5. **种子数据补全**：seed_options.py 补充 B站、微信视频号平台选项
6. **类型一致性修复**：MaterialLib filterStatus 统一使用 null 而非空字符串
7. **骨架提取确认**：Dismantle.vue 提取骨架前增加 ElMessageBox 确认弹窗

#### 第三轮：7项功能完善（commit: 478408c）

1. **裂变表单骨架筛选**：Fission.vue 增加平台+类型筛选下拉框，骨架选项显示平台标签，支持 filterable 搜索
2. **素材内容行展开预览**：MaterialLib.vue 表格增加 expandable row，点击展开查看素材内容
3. **平台筛选从选项表加载**：不再从当前页数据派生，确保所有平台都出现在筛选中
4. **裂变统计区分总数/页**：总数用 totalCount，分项统计标注"当前页"提示
5. **骨架删除级联警告**：删除前查询关联裂变数量，红色警告提示级联影响
6. **More 下拉菜单高亮**：当前页面在 More 子菜单时，More 导航项显示激活状态
7. **飞轮闭环 — 存为素材**：FissionRecords.vue 详情弹窗增加「存为素材」按钮，保存后自动跳转拆解页面

### 技术验证

- ✅ 前端构建成功（3 轮均通过）
- ✅ 后端路由加载正常
- ✅ 级联删除逻辑验证通过
- ✅ 全局错误拦截器不影响现有组件错误处理
- ✅ ECharts 趋势图正常渲染

### Git 记录

```
478408c feat: 7项功能完善 — 骨架筛选/内容预览/飞轮闭环/统计修正/删除警告/导航高亮
687d8c4 feat: 6项优化 — 级联删除/全局拦截器/页面标题/空状态/平台数据/类型修复
98dcb58 feat: 8项功能完善 — 状态流转/搜索/刷新/趋势图/复制/标签关联
```
